import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Hide the Next.js development tools badge (bottom-left in `next dev`). */
  devIndicators: false,
  poweredByHeader: false,
};

export default nextConfig;
